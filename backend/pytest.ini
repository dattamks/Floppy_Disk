[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
# Deactivated features (Drive-focus pivot) are not collected — see docs/deactivated-features.md
addopts = --ignore=deactivated
